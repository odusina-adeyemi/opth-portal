version: '3.8'

services:
  db:
    image: postgres:15-alpine
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_USER: default
      POSTGRES_PASSWORD: YiqnVtH3a0LO
      POSTGRES_DB: verceldb

  app:
    build: .
    ports:
      - '3000:3000'
    environment:
      DATABASE_URL: 'postgres://default:YiqnVtH3a0LO@ep-empty-glade-a6n7sny1-pooler.us-west-2.aws.neon.tech:5432/verceldb?sslmode=require'
    depends_on:
      - db

volumes:
  postgres_data:
