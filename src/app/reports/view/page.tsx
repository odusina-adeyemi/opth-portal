import React from 'react';
import KeyReferringProviders from '../_components/KeyReferringProviders';

const ViewReportsPage = async () => {
  // Placeholder for now. Each report I think will have its own page.tsx since
  // each report will require their own db calls and logic

  // what to fetch for Key Referring Providers?
  // fetch for patients based on when created_at is within 4 weeks

  // compare patients to find the most and least referring providers

  // change date
  return null;
  // @ts-ignore
  // <KeyReferringProviders
  //   noReferrals={noReferralProviders}
  // referralProviders={referralProviders}
  // />
};

export default ViewReportsPage;
